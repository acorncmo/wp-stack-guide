{# page.twig -- Generic page template #}
{% extends 'base.twig' %}

{% block content %}
  <article class="py-16 px-6 max-w-3xl mx-auto">
    <h1 class="font-display text-4xl mb-8">{{ post.title }}</h1>
    <div class="prose font-sans">
      {{ post.content }}
    </div>
  </article>
{% endblock %}
